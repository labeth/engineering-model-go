apiVersion: v1
kind: Service
metadata:
  name: risk-scorer
spec:
  selector:
    app: risk-scorer
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  type: {{ .Values.service.type }}
