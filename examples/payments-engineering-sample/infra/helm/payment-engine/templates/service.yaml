apiVersion: v1
kind: Service
metadata:
  name: payment-engine
spec:
  selector:
    app: payment-engine
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  type: {{ .Values.service.type }}
