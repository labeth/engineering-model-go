apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: payments-apps
  namespace: flux-system
  annotations:
    engmodel.dev/owner-unit: FU-GITOPS-OPERATIONS
    engmodel.dev/runtime-description: applies and reconciles HelmRelease definitions for checkout, payment, and risk services
spec:
  interval: 5m
  path: ./infra/flux/releases
  prune: true
  sourceRef:
    kind: GitRepository
    name: payments-sample-source
  wait: true
